# CDM Forensic Research Engine v2.3 - Dependencies
# Install with: pip install -r requirements.txt

# Core dependencies
requests>=2.28.0
jsonschema>=4.0.0

# Testing
pytest>=7.0.0

# NOTE: anthropic package removed in v2.0
# This platform now uses Claude Code extension (VS Code) for research execution
# No external API key required with Claude Max subscription
